import { Screen, Header, Form } from './Components';

export const Purchase = () => (
    <Screen>
        <Header title='Mckee Pure Water' content='Purchase Screen' />
        <Form />
    </Screen>
);

export default Purchase;
