import { Segment, Header } from 'semantic-ui-react';

const CustomerHistory = (props) => (
    <Segment raised>
        <Header block size='huge' content='Customer History' />
    </Segment>
);

export default CustomerHistory;
