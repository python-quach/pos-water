import { Segment, Header } from 'semantic-ui-react';

const AdminScreen = (props) => (
    <Segment raised>
        <Header block size='huge' content='Admin Screen' />
    </Segment>
);

export default AdminScreen;
